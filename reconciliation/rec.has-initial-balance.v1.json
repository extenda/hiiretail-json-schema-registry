{
  "id": "rec.has-initial-balance.v1",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HasInitialBalance",
  "description": "Parameter to define if reconciliation has initial balance",
  "type": "object",
  "required": ["hasInitialBalance"],
  "properties": {
    "hasInitialBalance": {
        "type": "boolean",
        "description": "Parameter to define initial balance"
    }
  }
}
