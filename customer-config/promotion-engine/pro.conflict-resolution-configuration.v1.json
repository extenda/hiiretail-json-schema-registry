{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/extenda/hiiretail-json-schema-registry/master/customer-config/promotion-engine/pro.conflict-resolution-configuration.v1.json",
  "title": "Promotion Engine conflict resolution configuration",
  "description": "Promotion Engine conflict resolution configuration",
  "type": "object",
  "additionalProperties": false,
  "$defs": {
    "Type": {
      "description": "Promotion type identifier used to declare relations (for example, a promotion rule or category name).",
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    },
    "Relation": {
      "description": "Defines how conflicts between two promotion types are handled.",
      "type": "object",
      "required": [ "left", "right", "strategy" ],
      "properties": {
        "left": {
          "description": "The left-hand promotion type in the relation.",
          "$ref": "#/$defs/Type"
        },
        "right": {
          "description": "The right-hand promotion type in the relation.",
          "$ref": "#/$defs/Type"
        },
        "strategy": {
          "description": "Conflict resolution strategy between the left and right types: LEFT_BEFORE_RIGHT applies the left type before the right one; LEFT_DISQUALIFIES_RIGHT prevents the right type from applying when the left type is present.",
          "type": "string",
          "enum": [
            "LEFT_BEFORE_RIGHT",
            "LEFT_DISQUALIFIES_RIGHT"
          ]
        }
      }
    }
  },
  "required": [ "types", "relations" ],
  "properties": {
    "types": {
      "description": "List of all promotion types that can appear in conflict relations.",
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "items": { "$ref": "#/$defs/Type" }
    },
    "relations": {
      "description": "Pairwise relations describing how conflicts between promotion types should be resolved.",
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "items": { "$ref": "#/$defs/Relation" }
    }
  }
}
