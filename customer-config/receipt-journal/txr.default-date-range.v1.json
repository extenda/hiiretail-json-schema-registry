{
  "$id": "txr.default-date-range.v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DefaultDateRange",
  "description": "Default selected date range to filter receipts for the receipt journal.",
  "type": "object",
  "properties": {
    "defaultDateRange": {
      "type": ["integer"],
      "description": "Default selected date range to filter receipts for the receipt journal.",
      "minimum": 1,
      "maximum": 30,
      "default": 14
    }
  }
}
