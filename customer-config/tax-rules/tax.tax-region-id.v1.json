{
  "$id": "tax.tax-region-id.v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TaxRegionId",
  "description": "Tax region for a country",
  "type": "object",
  "required": [
    "taxRegionId"
  ],
  "properties": {
    "taxRegionId": {
      "type": [
        "null",
        "string"
      ],
      "description": "Tax region for a country"
    }
  }
}