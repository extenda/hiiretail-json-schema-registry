{
  "$id": "sre.failed-events.v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Failed Events",
  "description": "Customer Event/Message Processing Failure Notifications",
  "type": "object",
  "properties": {
    "correlationId": {
      "type": "string",
      "description": "Unique identifier for the event"
    },
    "businessUnitId": {
      "type": "string",
      "description": "Identifier for the specific business unit"
    },
    "title": {
      "type": "string",
      "description": "Human readable title describing the failure"
    },
    "errorMessage": {
      "type": "string",
      "description": "Human readable explanation of the failure. If a translation cannot be found, a generic message is returned.",
      "default": "Specific error could not be determined."
    },
    "entityId": {
      "type": "string",
      "description": "Identifier of the entity that failed to process"
    },
    "entityType": {
      "type": "string",
      "description": "The entity type of the failed event",
      "enum": ["Item", "Promotion", "Plulist"]
    }
  },
  "required": ["correlationId", "businessUnitId", "title", "errorMessage", "entityId", "entityType"],
  "additionalProperties": false
}
