{
  "$id": "plt.private.notification.release-verification-requests.v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Release Verification Requests",
  "description": "Request for tenants to verify a new product release in a safe environment.",
  "type": "object",
  "required": [
    "eventId",
    "product",
    "version"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "description": "Unique identifier for the verification request"
    },
    "product": {
      "type": "string",
      "description": "Name of the product to verify"
    },
    "version": {
      "type": "string",
      "description": "Version of the product to verify"
    }
  },
  "additionalProperties": false
}
